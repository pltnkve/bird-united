apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-ums
  namespace: twitter-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-ums
  template:
    metadata:
      labels:
        app: mysql-ums
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          ports:
            - containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: mysql-root-password
            - name: MYSQL_DATABASE
              value: "ums"
          volumeMounts:
            - name: mysql-storage
              mountPath: /var/lib/mysql
      volumes:
        - name: mysql-storage
          persistentVolumeClaim:
            claimName: mysql-ums-pvc
